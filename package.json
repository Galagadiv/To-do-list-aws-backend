{
  "name": "to-do-list-backend",
  "version": "1.0.0",
  "main": "handler",
  "scripts": {
    "deploy": "serverless deploy",
    "deploy:fn": "serverless deploy function --function login"
  },
  "dependencies": {
    "cookie": "^0.5.0",
    "openid-client": "^5.3.4"
  }
}
